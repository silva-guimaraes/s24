

[   ( N -- N 2 -- N )
    2   -> Divisor
    [ ] arr
    swp
    loop dup 1 > do

        dup Divisor mod 0 = ? .if .else

            .if 
                ( [ ] N -- N [ ] -- N [ Divisor ] -- [ Divisor ] N )
                swp
                [ Divisor ] arr app
                swp
                Divisor / ;
            .else Divisor ++ -> Divisor ;
        .end

    over pop
] !-> FactorPrimes

42 FactorPrimes
